# CHANGELOG — TapTime

รูปแบบวันที่: ปี ค.ศ.

---

## [1.3.0] — 2026-08-04

เพิ่ม **ระบบ OT (ล่วงเวลา)** ทั้งระบบ ตาม `TapTime-OT-Spec.md`
เฟสนี้เก็บ **"ชั่วโมง" อย่างเดียว ยังไม่คิดเป็นเงิน** — คอลัมน์ฝั่งการจ่ายถูกสร้างไว้ในฐานข้อมูลแล้ว
เพื่อให้เฟสถัดไปไม่ต้อง migrate ซ้ำ และมีเทสต์ไล่ทั้งโปรเจกต์คอยกันไม่ให้มีโค้ดไปแตะ

### ⚠️ ต้องทำก่อนใช้งาน

1. รัน `supabase/migration-005-overtime.sql` บน Supabase (รันซ้ำได้)
2. เปิดเมนู **"ตั้งค่า OT"** แล้วตั้งกฎให้ตรงกับที่บริษัทใช้จริง — ค่าเริ่มต้นคือ
   นับหลังเลิกงาน · ผ่อนผัน 15 นาที · ขั้นต่ำ 30 นาที · ปัดลงทีละ 30 นาที ·
   เพดาน 4 ชม./วัน · โหมด "นับก่อน แล้วค่อยอนุมัติ"

### 🧮 แกนคำนวณ

- `lib/api/overtime.ts` — `computeOt()` เป็น **pure function** ทดสอบได้โดยไม่ต้องต่อฐานข้อมูล
  ลำดับขั้นตายตัว: กันกะข้ามคืน → เวลาดิบ → หักพัก → ปัดเศษ → ขั้นต่ำ → เพดาน → แยกตามโหมด
- **5 โหมด**: `off` · `auto` · `request_after` · `request_before` · `admin_only`
- **ไม่รองรับกะข้ามคืนโดยตั้งใจ** — เวลาออก ≤ เวลาเข้า คืน 0 พร้อม note `cross_midnight_unsupported`
  ไม่เดา `+1440` นาที
- เก็บทั้ง **ค่าดิบ** (`otMinutesRaw`) และ **ค่าที่นับได้** (`otMinutes`) เพื่อตรวจย้อนหลัง
- **snapshot `otPolicyId`** ลงทุกแถวลงเวลา — บันทึกนโยบายทีไรสร้างแถวใหม่เสมอ ไม่ทับของเก่า

### 🔀 การเปลี่ยนแปลงที่กระทบข้อมูลเดิม

- **`workHours` นิยามใหม่: ไม่รวม OT อีกต่อไป** ยึดสมการ
  `workHours + otMinutes/60 = (เวลาออก − เวลาเข้า) − เวลาพัก`
  ของเดิมนับ OT ซ้ำสองที่ ถ้าเอาไฟล์ export ไปคิดเงินต่อจะจ่ายเกิน
  (หัก "นาทีที่นับได้" ออก ไม่ใช่ตัดที่ `workEnd` เพื่อไม่ให้มีนาทีไหนหายจากทั้งสองช่อง)
- **`otMinutes` นิยามใหม่: "นาทีที่อนุมัติแล้ว"** ข้อมูลเก่าที่คิดด้วยกฎเดิม
  (ไม่มีผ่อนผัน/ขั้นต่ำ/เพดาน และไม่เคยผ่านการอนุมัติ) ถูกย้ายไป `otMinutesRaw`
  แล้วรีเซ็ต `otMinutes` เป็น 0 พร้อม note `legacy_pre_ot_policy`
- **ทำงานวันหยุดไม่นับ "มาสาย"** อีกต่อไป
- **แท็บ "การลา" ฝั่งพนักงานเปลี่ยนเป็น "คำขอ"** รวมใบลา ใบ OT และใบแก้เวลาไว้ที่เดียว
  (ปุ่มขอแก้เวลาย้ายออกจากแท็บประวัติ)
- ลบ config ขยะ `otRateType` / `otFlatHours` / `otFlatAmount` ที่ไม่มีโค้ดอ่าน

### ✨ ของใหม่

**ฝั่งแอดมิน — 3 เมนูใหม่**

- **ตั้งค่า OT** — 4 การ์ด (โหมด · กฎการนับ · เพดาน · วันหยุด) พร้อม **กล่องพรีวิวสด**
  ที่คำนวณให้ดูทีละขั้นด้วยสูตรเดียวกับเซิร์ฟเวอร์
- **อนุมัติ OT** — ตัวกรอง (สถานะ/ช่วงวัน/สาขา/ชื่อ) · แก้จำนวนนาทีก่อนอนุมัติ ·
  เลือกหลายรายการอนุมัติพร้อมกัน
- **รายงาน OT** — สรุปรายคนแยกตามประเภทวัน + ปุ่มคัดลอกตารางเป็น TSV ไปวางใน Excel ได้ทันที

**หน้าเดิมที่เพิ่มคอลัมน์ OT** — เช็คชื่อเข้า-ออก (ดิบ / นับได้ / สถานะ + ปุ่มคีย์ OT ให้) ·
รายงานรายวัน · รายงานเดือน · ไฟล์ Excel

**ฝั่งพนักงาน**

- กล่องสรุปหลังเช็คเอาท์แยก "เวลาทำงานปกติ" กับ "OT" พร้อม badge สถานะ
- ยื่น/ยกเลิกใบขอ OT ได้เอง (เฉพาะโหมดที่ยื่นได้) + สถิติ OT เดือนนี้ แยกอนุมัติแล้ว/รออนุมัติ
- **ห้ามส่งฟิลด์เกี่ยวกับเงินไปฝั่งพนักงาน** — มีเทสต์ตรวจ payload ทั้งก้อน

**ทำงานวันหยุด** — ปลดล็อก `empCheckIn` ที่เดิมปฏิเสธวันหยุดทุกกรณี
เพิ่มใบชนิด `holiday_work` ที่ **ยื่นได้ทุกโหมด ยกเว้น `off`** —
ถ้าไม่ทำแบบนี้ โหมด `auto`/`admin_only` + `holidayNeedsRequest` จะเป็นทางตัน
เพราะพนักงานหาทางสร้างใบอนุมัติไม่ได้เลย (สเปกเดิมไม่ได้ครอบคลุมกรณีนี้)

**แจ้งเตือน** — เพิ่ม `ot_submitted` (→ แอดมินทุกคน) และ `ot_decided` (→ เจ้าของใบ)

### 🐛 แก้บั๊ก

- **ใบ OT ค้างรออนุมัติทั้งที่ไม่มี OT** — ถ้าแอดมินอนุมัติคำขอแก้เวลาให้ย้อนกลับ
  จนไม่เหลือ OT ใบที่ระบบสร้างไว้จะค้างอยู่ ตอนนี้ถูกยกเลิกอัตโนมัติ
- **ใบซ้ำเมื่อเช็คเอาท์ซ้ำ** — ใบที่ระบบสร้างใช้ upsert ตามใบเดิม ไม่ใช่ insert ทุกครั้ง
  (ไม่งั้นการอนุมัติคำขอแก้เวลาจะชน unique index `(empId, date, kind)`)

### 📋 ที่ยังตรวจไม่ได้

- ยังไม่เคยรัน `next build` / `next dev` จนจบใน sandbox — ตรวจด้วย `tsc --noEmit` + ชุดทดสอบ 215 ข้อแทน
- ยังไม่ได้ทดสอบกับข้อมูลจริงบน Supabase

---

## [1.2.0] — 2026-08-01

เวอร์ชันนี้คือการ **ย้ายระบบทั้งก้อน** จาก Google Apps Script + Google Sheets (V1.1)
มาเป็นเว็บแอปจริงบน **Next.js 14 + Supabase (PostgreSQL) + Vercel**
พร้อมปิดช่องโหว่ด้านความปลอดภัยที่ติดมาจากของเดิม และปรับ UX อีกหลายจุด

> **หลักการตอนย้าย:** UI เดิมถูกยกมาทั้งดุ้นโดยไม่แก้แม้แต่บรรทัดเดียว
> (ใช้ shim จำลอง `google.script.run` ทับ `fetch('/api/rpc')`)
> การเปลี่ยนหน้าจอทั้งหมดที่อยู่ในรายการนี้เกิดขึ้น **หลังจาก** ย้ายเสร็จ ตามที่ร้องขอเพิ่ม

---

### 🏗 โครงสร้าง / สถาปัตยกรรม

| หัวข้อ | เดิม (V1.1) | ใหม่ (V1.2) |
|---|---|---|
| ฐานข้อมูล | Google Sheets | Supabase (PostgreSQL) 12 ตาราง + RLS |
| Backend | Apps Script `.gs` | Next.js API route (TypeScript) |
| การเรียก backend | `google.script.run` | `POST /api/rpc` (`{fn, args}` + ชั้นตรวจสิทธิ์) |
| ตารางพนักงาน | ชีต `Employees` | ตาราง `profiles` + Supabase Auth |
| Export Excel | Drive + UrlFetch | SheetJS ในหน่วยความจำ |
| งานตามเวลา | Apps Script trigger | Vercel Cron (ป้องกันด้วย `CRON_SECRET`) |
| Deploy | Apps Script deployment | GitHub → Vercel (auto deploy ทุก push) |

- เก็บเวลาเป็น `text` รูปแบบ `'HH:mm'` และวันที่เป็น `date` — เลี่ยงปัญหา Date เพี้ยนข้ามโซนเวลา
- ตั้งชื่อคอลัมน์แบบ camelCase ครอบด้วย `"` ใน Postgres เพื่อให้ payload ที่ frontend เดิมส่งมาใช้ได้ทันที
- แปลงข้อมูลเดิมจาก `TapTime.xlsx` เป็น `supabase/seed.sql` ด้วย `scripts/xlsx-to-sql.py`

---

### 🔐 ความปลอดภัย (จุดสำคัญที่สุดของเวอร์ชันนี้)

- **ยกเลิกลิงก์ `?token=` ทั้งหมด** — เดิมใครได้ลิงก์ไปก็เช็คอินแทนกันได้ ตอนนี้ต้อง login
- **หน้า login เดียวสำหรับทุกคน** แยกสิทธิ์ด้วย `role`
  - admin + เดสก์ท็อป → หน้าจัดการ
  - admin + มือถือ → หน้าพนักงาน
- **จดจำฉันไว้** 30 วัน (ถ้าไม่ติ๊ก = session cookie อายุไม่เกิน 12 ชม.)
- **WebAuthn / passkey** (Face ID / ลายนิ้วมือ / PIN ล็อกหน้าจอ) บังคับใช้ทุกครั้งที่เช็คอิน–เช็คเอาท์
  ผูกกับอุปกรณ์ ใช้ `userVerification: 'required'` + ตรวจ signature counter กันโคลน
- **`applyIdentity()`** — เขียนทับ `args[0]` ด้วย `empId` จาก session เสมอ
  client ปลอมตัวตนของคนอื่นไม่ได้แม้จะแก้ payload เอง
- **แก้ช่องโหว่ GPS ที่ติดมาจาก V1.1** — เดิม `NaN > radius` ให้ค่า `false` ทำให้ **ข้ามการตรวจ geofence ทั้งหมด**
  ถ้าไม่ส่งพิกัดมา ตอนนี้เปลี่ยนเป็น fail-closed + ตรวจความแม่นยำ + บันทึก audit log
- อีเมลเชิญตั้งรหัสผ่านอัตโนมัติเมื่อเพิ่มพนักงานพร้อมอีเมล + ปุ่ม **"ส่งอีเมลเชิญใหม่"**
- สิทธิ์ Admin อ่านสดจากฐานข้อมูลทุกครั้ง (เดิมอ่านจาก cookie 30 วัน → ปลด/เพิ่มสิทธิ์ไม่มีผลทันที)
- เพิ่ม `scripts/check-secrets.mjs` — build/dev/test จะล้มทันทีถ้ามีคีย์จริงหลุดเข้าไฟล์ที่ track ใน git

---

### ✨ ฟีเจอร์ใหม่

**ปุ่มสไลด์เช็คอิน / เช็คเอาท์ (จุดขายของแอป)**
- ทรงแคปซูล พื้นไล่สีเข้ม ตัวหนังสือขาว ลูกบิดกลมเงาลึก
- ตัวช่วยดึงสายตา: ข้อความกระพริบ + ลูกศร `›››` วิ่ง + วงแหวนเต้นรอบลูกบิด + แสงกวาดผ่านปุ่ม
- กดที่ไหนบนแถบก็ลากได้ (เดิมต้องจิ้มให้ตรงลูกบิด), ข้อความจางลงตามระยะที่ลาก, สั่นเบา ๆ เมื่อลากสุด
- เคารพ `prefers-reduced-motion`

**การแจ้งเตือนในแอป**
- ยื่นคำขอลา → แจ้งแอดมินทุกคน / อนุมัติแล้ว → แจ้งกลับพนักงาน
- ไอคอนกระดิ่ง + badge จำนวนที่ยังไม่อ่าน ทั้งฝั่งแอดมินและพนักงาน
- เสียงกระดิ่ง (สร้างด้วย WebAudio ไม่ต้องมีไฟล์เสียง) ดังครั้งแรกที่เปิดแอปเมื่อมีรายการค้าง
- Cron รายวันเตือนแอดมินเมื่อมีคนขาดงาน

**หน้าจัดการพนักงาน**
- ค้นหา + filter + pagination (10 / 20 / 50 / 100 — ค่าเริ่มต้น 20)
- ฟิลด์ใหม่: ชื่อเล่น, วันเกิด, ที่อยู่, เพศ, สัญชาติ พร้อมจัดลำดับฟอร์มใหม่
- อัปโหลดรูปแล้วย่อเป็น 150×150 (ครอปกึ่งกลาง) ก่อนบันทึก
- ให้/ถอนสิทธิ์ Admin ได้จากหน้านี้ พร้อมไอคอนโล่บอกว่าใครเป็นแอดมิน
- ตารางสะอาดขึ้น: ซ่อนอีเมล เบอร์โทร และคอลัมน์ตำแหน่ง, ปุ่มจัดการเป็นไอคอน + tooltip
- จัดการอุปกรณ์ passkey ของพนักงาน + ยกเว้น passkey เป็นราย ๆ ได้

**ตั้งค่าวันลา**
- Assign ประเภทการลาให้รายคนหรือทุกคนได้ (รองรับพนักงานอายุงานเกิน 1 ปีที่ได้พักร้อนเพิ่ม)
- โควตาเฉพาะบุคคล + ติ๊กเลือกว่าจะโชว์จำนวนวัน/ปี ในหน้าพนักงานหรือไม่

**ฝั่งพนักงาน**
- อัปโหลดรูปโปรไฟล์ตัวเองได้ (ย่อ 150×150 เหมือนฝั่งแอดมิน) แสดงผลที่ 60×60
- เมนูวันหยุดเหลือแค่ปฏิทิน (เอากล่อง "วันหยุดที่จะถึง" ออก)

**อื่น ๆ**
- โลโก้ + favicon + apple-touch-icon + PWA manifest (ติดตั้งเป็นแอปบนมือถือได้)
- เมนูวันหยุดฝั่งแอดมิน: ย้ายการเพิ่ม/แก้ไข/ลบ เข้าไปอยู่ในกล่องปฏิทิน (เอาตารางออก)

---

### 🐛 แก้บั๊ก

- **สูตรชั่วโมงทำงานผิด 2 จุด** — ตัวนับสดไม่หักเบรกแต่ยอดรวมหัก (หายไป 1 ชม.)
  และหักเบรกโดยไม่ดูเกณฑ์ (กะ 3 ชม. เหลือ 2 ชม.)
  แก้ด้วย `computeWorkHours()` ที่ทั้ง frontend และ backend ใช้ร่วมกัน + เกณฑ์ `breakAfterHours`
- **หน้าจอมือถือไม่เต็มพอดี** — `<meta name="viewport">` หายไปตอนพอร์ต
  (ของเดิม Apps Script ใส่ให้ผ่าน `doGet().addMetaTag()` ไม่ได้อยู่ในไฟล์ HTML)
- **ลิงก์ในอีเมลเชิญชี้ไป `localhost:3000`** — `NEXT_PUBLIC_*` ถูกฝังตอน build
  แก้ให้ `appUrl()` อ่านจาก `x-forwarded-host` ก่อน + ตัด path ที่ติดมากับ URL
- **กันกดปุ่มซ้ำทุกฟอร์ม** ทั้งฝั่งแอดมินและพนักงาน (ปิดปุ่ม + ขึ้น "กำลังบันทึก...")
- **กันยื่นลาซ้ำ / ลาทับช่วงวันที่ลาไปแล้ว**
- **ยกเครื่อง loading ฝั่งแอดมินทั้งหมด** — ตัวโหลดเคยเยื้อง 115px เพราะ `position:fixed`
  ไม่ได้หักความกว้าง sidebar, เปลี่ยน filter แล้วตารางเดิมค้าง, กดเมนูรัวได้
- ตัวโหลด 60×60 กลางจอตอนเปิดแอป

---

### ⚠️ สิ่งที่ต้องทำเองหลังอัปเดต

1. รัน `supabase/migration-004-notify-worktime.sql` บน Supabase
2. เพิ่ม env `CRON_SECRET` บน Vercel
3. ตรวจว่า `NEXT_PUBLIC_APP_URL` เป็น **origin เปล่า ๆ** (ไม่มี `/admin` ต่อท้าย) แล้ว Redeploy
4. เพิ่ม URL ของ Vercel ใน Supabase → Authentication → URL Configuration (Site URL + Redirect URLs)
5. **เปลี่ยน GitHub repo เป็น Private** — `seed.sql` และ `fixture.json` มีข้อมูลส่วนบุคคลของพนักงานจริง

### 📋 ที่ยังตรวจไม่ได้

- ยังไม่เคยรัน `next build` / `next dev` จนจบใน sandbox (timeout) — ตรวจด้วย `tsc --noEmit` + ชุดทดสอบ 153 ข้อแทน
- WebAuthn ต้องทดสอบบนมือถือจริงผ่าน HTTPS เท่านั้น

---

## [1.1.0] — ระบบเดิม

Google Apps Script + Google Sheets — ดูรายละเอียดที่ `TapTime-Master-Spec.md`
